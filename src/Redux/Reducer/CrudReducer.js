import React from "react";
import { crudConst } from "../Actions/CrudAction/CrudConst";
import { user_api } from "../../Api/apiUrl";

const initialState = {
  users: [],
};

const CrudReducer = (state = initialState, action) => {
  switch (action.type) {
    case `${user_api}_`
  }
};

export default CrudReducer;
